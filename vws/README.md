# vws - Fast recursive search with context

A blazing fast grep-like tool written in Rust. Shows 4 lines before and 7 lines after each match.

## Installation

```bash
cargo build --release
sudo cp target/release/vws /usr/local/bin/
